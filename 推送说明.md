# 推送到 GitHub 的说明

当前 gh 账号 sisyphus-02 的 token 没有 `CreateRepository` 权限，无法自动建仓库。
请任选一种方式完成开源：

## 方式 A：网页创建 + 命令行推送（推荐）
1. 打开 https://github.com/new ，登录你的 GitHub 账号
2. 仓库名填 `dsh-gamehub`，选 Public，勾选 "Add a README file"（可选），点 Create
3. 在本目录执行：
   git remote add origin https://github.com/<你的用户名>/dsh-gamehub.git
   git push -u origin main

## 方式 B：换一个有权限的 gh 账号
gh auth login   # 登录有建仓权限的账号
gh repo create dsh-gamehub --public --source=. --remote=origin --push

## 方式 C：作为文件包分发
把整个 gamehub-share 目录打包（zip）发给朋友 / 传到任意平台：
   zip -r dsh-gamehub.zip .
朋友解压后按 README.md 的"安装"章节安装即可，无需 GitHub。
